extend layout

block content
  div(class="home")
    h2 You can browse the inventory based on the laptop model, brand, and type.
    p Select on the categories to view your inventory, create new entries, update, and remove existing one.
  
    if data
      ul(class="list-group")
        li(class="list-group-item list-group-item-dark") Laptops in inventory: !{data.laptop}
        li(class="list-group-item list-group-item-dark") Brands in inventory: !{data.brand}
        li(class="list-group-item list-group-item-dark") Types in inventory: !{data.type}